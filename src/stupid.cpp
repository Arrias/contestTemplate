#include <bits/stdc++.h>

#define ol(ph) cout << ph << "\n"
#define ll long long
#define ld long double

using namespace std;

mt19937_64 rnd(std::chrono::system_clock::now().time_since_epoch().count());

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);


    return 0;
}